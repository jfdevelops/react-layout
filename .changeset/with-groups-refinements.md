---
"@jfdevelops/react-layout": patch
---

Rename `createResourceLinks.withGroup` to `withGroups`, add a generated `id` to each returned group, and improve grouped link autocomplete when mapping over results.
