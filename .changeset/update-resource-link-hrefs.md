---
"@jfdevelops/react-layout": minor
---

Update `createResourceLinks` to return structured href metadata with separate `given`, `full`, and optional `hash` values. Resource link config now supports independent `href` and `hash` options, including resource-aware functions, and the implementation has been extracted into its own module.
