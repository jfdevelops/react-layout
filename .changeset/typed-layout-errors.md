---
'@jfdevelops/react-layout': minor
---

Add a typed `ReactLayoutError` hierarchy (`InvalidPathError`, `InvalidResourceError`, `InvalidSubResourceError`, `InvalidComponentError`, `MissingPathVariableError`, `InvalidConfigError`) so layout lookups fail with structured, actionable errors instead of bare `Error` strings.
