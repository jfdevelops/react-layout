---
"@jfdevelops/react-layout": patch
---

Add `createResourceLinks.withGroup` for building grouped navigation links with full IDE autocomplete support. Group `label` is optional and defaults to `null`.
