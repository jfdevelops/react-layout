---
'@jfdevelops/react-layout': minor
---

Allow `createResourceLinks` and `createResourceLinks.withGroups` to use arbitrary property names while preserving each key's literal type in `href` and `hash` callbacks.
